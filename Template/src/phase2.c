#include "phase2.h"
//Phase 2
/*

void master(){

	//open the directory /MapperInput
	//get number of files in /MapperInput, n


	//create pipe

	//start child process
	//fork()
	//if child
		//start the reducer
		//run reduce(pipe)		
		return;

	//if parent
		//continue


	for (int i = 0; i < n; i++){
		
		

		//Create a pipe
		// map(i) -> reduce
		//pass output to reduce		

		//start child process
		//fork()
		//if child
			//run map(i,pipe)
			return;
		//if parent
			//continue
	}	
	return;
}		

void map(int i){
	//open Mapper_i.txt from /MapperInput
		
	//find the number of words corresponding to each letter of the alphabet from the file
	//case insensitive

        //write the list to sdout
	return;
}



No output

The grading will be carried out based on the per letter word
count algorithm, pipe setup and usage.

/*
